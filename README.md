# ts-standalone-project

Minimal TypeScript standalone project with .env setup using `dotenv`.

Quick start

Install dependencies:

```bash
npm install
```

Run in development (auto-restart):

```bash
npm run dev
```

Build and run:

```bash
npm run build
npm start
```

Environment

Copy `.env.example` to `.env` and edit values as needed.
